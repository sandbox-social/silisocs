## Run Instructions
In project folder (`mastodon-sim/`) run `python src/sim/main.py`

N.B.

- Output writes to `src/scenarios/election/output` by default

## Post-run Analysis
Post-run analysis scripts are in `src/sim/analysis_utils/`