# @package _global_

scenario_name: virtual_space
jobname_format: "N${num_agents}_T${num_steps}_${experiment_name}_${run_name}"
num_agents: 4
num_steps: 5
run_name: run1
seed: 1
output_rootname: ""

setting:
  name: Virtual Commons
  background:
    - Participants share a small virtual space with multiple rooms.
    - They can move around, observe who is nearby, and talk to co-located agents.

event:
  name: Open exploration session
  context: |
    Participants are exploring a virtual commons. They can look around, move
    between rooms, talk to someone present in the same room, or wait.

data: {}
