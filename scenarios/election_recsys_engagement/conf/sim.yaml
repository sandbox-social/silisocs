llm:
  name: qwen3.5-4b
  api_base: http://localhost:30000/v1
  api_key: test-key

memory_backend: list
action_mode: generic
tool_calling:
  mode: multi
engine:
  preset: base
  action_loop:
    built_in: open_ended
    params:
      max_actions: 20
      finished_action_signal: FINISHED
      done_token: FINISHED
  flow_routing:
    flow_order:
    - fixed_pre
    - default
